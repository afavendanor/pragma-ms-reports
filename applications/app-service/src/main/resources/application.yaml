spring:
  application:
    name: pragma-ms-reports
  webflux:
    base-path: /reports
  profiles:
    include: sqs, dynamo

logging:
  level:
    co.com.pragma: DEBUG

server:
  port: 8083
  servlet:
    context-path: /reports

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: swagger-ui.html

